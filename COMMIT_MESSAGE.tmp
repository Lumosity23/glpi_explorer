fix(list): Correction de l'accès aux attributs pour SimpleNamespace

- Remplacement de `item.get("key")` par `getattr(item, "key", "N/A")`.
- Résout l'AttributeError qui se produisait lors de l'utilisation de la commande `list`.