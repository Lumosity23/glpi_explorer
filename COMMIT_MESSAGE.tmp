fix(trace): Correction d'une faute de frappe dans l'appel de la méthode

- Remplacement de `get_connection_for_socket` par `find_connection_for_socket`.
- Résout l'AttributeError qui se produisait lors de l'utilisation de la commande `trace`.